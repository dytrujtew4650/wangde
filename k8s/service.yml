apiVersion: v1
kind: Service
metadata:
  name: gert
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gert"
    port: 8080
  selector:
    app: gert
