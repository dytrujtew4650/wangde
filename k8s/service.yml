apiVersion: v1
kind: Service
metadata:
  name: wegd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wegd"
    port: 8080
  selector:
    app: wegd
