apiVersion: v1
kind: Service
metadata:
  name: houk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "houk"
    port: 8080
  selector:
    app: houk
