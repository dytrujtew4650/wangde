apiVersion: v1
kind: Service
metadata:
  name: sdou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sdou"
    port: 8080
  selector:
    app: sdou
