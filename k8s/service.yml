apiVersion: v1
kind: Service
metadata:
  name: wood
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wood"
    port: 8080
  selector:
    app: wood
