apiVersion: v1
kind: Service
metadata:
  name: werf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "werf"
    port: 8080
  selector:
    app: werf
