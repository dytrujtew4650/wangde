apiVersion: v1
kind: Service
metadata:
  name: gmoh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gmoh"
    port: 8080
  selector:
    app: gmoh
