apiVersion: v1
kind: Service
metadata:
  name: four
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "four"
    port: 8080
  selector:
    app: four
