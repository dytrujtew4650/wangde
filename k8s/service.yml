apiVersion: v1
kind: Service
metadata:
  name: doer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "doer"
    port: 8080
  selector:
    app: doer
