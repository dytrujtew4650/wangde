apiVersion: v1
kind: Service
metadata:
  name: wdou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wdou"
    port: 8080
  selector:
    app: wdou
