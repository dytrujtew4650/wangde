apiVersion: v1
kind: Service
metadata:
  name: souy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "souy"
    port: 8080
  selector:
    app: souy
