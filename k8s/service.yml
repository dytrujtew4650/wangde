apiVersion: v1
kind: Service
metadata:
  name: sunf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sunf"
    port: 8080
  selector:
    app: sunf
