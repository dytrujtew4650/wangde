apiVersion: v1
kind: Service
metadata:
  name: wsct
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wsct"
    port: 8080
  selector:
    app: wsct
