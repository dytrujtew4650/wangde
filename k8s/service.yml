apiVersion: v1
kind: Service
metadata:
  name: doou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "doou"
    port: 8080
  selector:
    app: doou
