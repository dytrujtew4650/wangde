apiVersion: v1
kind: Service
metadata:
  name: doyf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "doyf"
    port: 8080
  selector:
    app: doyf
