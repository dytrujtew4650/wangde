apiVersion: v1
kind: Service
metadata:
  name: good
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "good"
    port: 8080
  selector:
    app: good
