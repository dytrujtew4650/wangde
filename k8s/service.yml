apiVersion: v1
kind: Service
metadata:
  name: sougd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sougd"
    port: 8080
  selector:
    app: sougd
