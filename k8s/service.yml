apiVersion: v1
kind: Service
metadata:
  name: wtou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wtou"
    port: 8080
  selector:
    app: wtou
